# دعم العربية والإنجليزية في NESS

تمت إعادة بناء طبقة تعدد اللغات بحيث تعمل الواجهة بالكامل بالعربية والإنجليزية مع اتجاه RTL/LTR صحيح.

## المشكلة التي تم إصلاحها
الإصدار السابق كان يراقب تغييرات النصوص بـ `MutationObserver` ثم يعيد كتابة نفس `characterData` أثناء الترجمة دون تمييز التغيير الذي أنشأه محرك الترجمة نفسه. عند اختيار العربية كان ذلك قادرًا على إنشاء سلسلة متكررة من الـmutations واستهلاك خيط المتصفح الرئيسي حتى يبدو المتصفح متجمداً.

## الإصلاح
- إضافة state لكل Text Node وAttribute لتمييز النص الأصلي عن النص المعروض.
- تجاهل mutations التي أنشأها محرك الترجمة نفسه.
- تجميع التغييرات الجديدة وتشغيلها على دفعة واحدة عبر `requestAnimationFrame`.
- عدم إعادة الكتابة إذا كانت القيمة الحالية مطابقة للقيمة المطلوبة.
- تجهيز قاموس الترجمة مرة واحدة بدل إعادة بناء أنماط البحث مع كل نص.
- تطبيق اللغة المحفوظة قبل رسم الصفحة بواسطة `language-bootstrap.js` لمنع وميض LTR/RTL.
- إضافة RTL متكامل للـSidebar والجداول والنماذج والـTopology والـModals، مع إبقاء IP/MAC/Code/Logs باتجاه LTR لسلامة القراءة.
- تثبيت `value` لكل `<option>` حتى لا تؤثر ترجمة النص المرئي في القيم التي يرسلها النظام للـBackend.
- إضافة اختيار اللغة إلى Settings وإلى الملف الشخصي، مع بقاء زر EN/AR العام في جميع الشاشات.
- تحديث الرسوم البيانية والجداول والمحتوى الديناميكي عند تغيير اللغة.

## طريقة الاستخدام
يمكن تغيير اللغة من زر `EN / AR` الموجود أعلى الواجهة في كل الصفحات، أو من:

`Settings → General Settings → Interface Language`

يتم حفظ الخيار محليًا في المتصفح، وتستمر اللغة نفسها عند الانتقال بين صفحات NESS أو بعد إعادة فتح النظام.
